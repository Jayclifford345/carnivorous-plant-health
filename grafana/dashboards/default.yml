apiVersion: 1

providers:
  - name: greenhouse # A uniquely identifiable name for the provider
    folder: Greenhouse # The folder where to place the dashboards
    type: file
    options:
      path:
        /etc/grafana/provisioning/dashboards/